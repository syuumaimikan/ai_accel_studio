# Install a CUDA build of PyTorch matching your driver first.
# The transformers[kernels] extra asks pip for the matching HF kernels range.
transformers[kernels]>=5.14,<5.18
accelerate>=1.0
safetensors>=0.5
sentencepiece>=0.2
torchao>=0.17,<0.19
packaging>=24
# MSLK is optional and CUDA-version-specific. Run `python -m ai_accel_lab doctor`
# for the exact requirement and install source/index recommended by TorchAO.
